#include <bits/stdc++.h>
using namespace std;
int main(){
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
    int n;
    cin>>n;
    while(n--){
        long long  r,c;
        cin>>r>>c;
        if(r%2==0){

        }
    }
    fclose(stdin);
    fclose(stdout);
}